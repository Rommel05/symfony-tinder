{% extends 'base.html.twig' %}

{% block title %}Likes{% endblock %}
{% block stylesheet %}
    <link rel="stylesheet" href="{{ asset('css/style-form.css') }}">
{% endblock %}
{% block body %}
    {# {% for like in likes %}
        <div>
            {% if like is not null %}
                <p>{{ like.userA.name }}</p>
            {% else %}
                <p>No tienes nigún like</p>
            {% endif %}
        </div>
    {% endfor %} #}

    {% if likes is not empty %}
        {% for like in likes %}
            <img src="{{ asset('images/' ~ like.userA.imagePath) }}" alt="profile image" id="profile-image">
            <div class="profile-information">
                <h2>{{ like.userA.name }}</h2>
                {# <p>{{ usuario.id }}</p> #}
            </div>
        {% endfor %}
    {% else%}
        <p>No tienes ningún like</p>
    {% endif %}
{% endblock %}
